# Vagrant-Ansible-LEMP-Symfony
Vagrant file with ansible provision for a Symfony LEMP

* change the ip of the private network in the Vagrantfile
* change the domain name in the ansible/site.yml file
+ fire it up
